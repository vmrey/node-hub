name = "node-hub"
main = "src/index.js"
compatibility_date = "2026-09-06"

# KV 存储绑定：本地开发请保留占位 ID（如 local-kv-id），npm run deploy 会自动将其替换为 Cloudflare 真实 KV ID。
[[kv_namespaces]]
binding = "KV"
id = "local-kv-id"
